{
  "name": "cygha-live",
  "version": "1.0.0",
  "description": "E2E training",
  "main": "index.html",
  "scripts": {
    "cy:local:open": "cypress open --e2e --browser edge --config baseUrl=http://127.0.0.1:8080/",
    "cy:local:run": "cypress run --e2e --browser edge --config baseUrl=http://127.0.0.1:8080/"
  },
  "author": "Sylvain Viole",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^12.17.3",
    "http-server": "^14.1.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
